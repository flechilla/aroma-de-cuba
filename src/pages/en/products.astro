---
import BaseLayout from '@layouts/BaseLayout.astro';
import SectionLanding from '@components/SectionLanding.astro';
import { getCollection } from 'astro:content';
import { useTranslations } from '../../i18n/utils';

const lang = 'en' as const;
const t = useTranslations(lang);

const posts = (await getCollection('blog', ({ data, id }) =>
  !data.draft && id.startsWith('en/') && data.category === 'productos'
)).sort((a, b) => b.data.date.getTime() - a.data.date.getTime());
---

<BaseLayout title={t('section.productos.metaTitle')} description={t('section.productos.metaDescription')} lang={lang}>
  <SectionLanding
    heroTitle={t('section.productos.heroTitle')}
    heroDescription={t('section.productos.heroDesc')}
    intro={t('section.productos.intro')}
    posts={posts}
    lang={lang}
    category="productos"
  />
</BaseLayout>
