---
import BaseLayout from '@layouts/BaseLayout.astro';
import SectionLanding from '@components/SectionLanding.astro';
import { getCollection } from 'astro:content';
import { useTranslations } from '../i18n/utils';

const lang = 'es' as const;
const t = useTranslations(lang);

const posts = (await getCollection('blog', ({ data, id }) =>
  !data.draft && id.startsWith('es/') && data.category === 'gastronomia'
)).sort((a, b) => b.data.date.getTime() - a.data.date.getTime());
---

<BaseLayout title={t('section.gastronomia.metaTitle')} description={t('section.gastronomia.metaDescription')} lang={lang}>
  <SectionLanding
    heroTitle={t('section.gastronomia.heroTitle')}
    heroDescription={t('section.gastronomia.heroDesc')}
    intro={t('section.gastronomia.intro')}
    posts={posts}
    lang={lang}
    category="gastronomia"
  />
</BaseLayout>
